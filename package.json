{
  "name": "reqres-api-qa-assignment",
  "version": "1.0.0",
  "private": true,
  "description": "Postman/Newman API tests for the ReqRes QA assignment.",
  "scripts": {
    "validate:postman": "node scripts/validate-postman-files.mjs",
    "test:api": "node scripts/run-postman-tests.mjs",
    "test": "npm run validate:postman && npm run test:api"
  },
  "engines": {
    "node": ">=22"
  }
}
